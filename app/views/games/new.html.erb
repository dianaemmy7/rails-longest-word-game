<!--  The new action will be used to display a new random grid and a form. The form will be submitted (with POST) to the score action. -->

<h1>Welcome to the Longest Word Game</h1>
<br>
<h3>Choose from this grid:</h3>
<ul class="list-group list-group-horizontal list-inline">
<% @letters.each do |letter| %>
  <li class="list-group-item"><%= letter.capitalize %></li>
<% end %>
</ul>
<br>
<form action="/score" method="post">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= hidden_field_tag 'grid', @letters.join() %>
  <label for="exampleInputPassword1">What is the longest word you can find?</label>
  <div class="form-group form-inline">
    <input type="text" class="form-control" name="word" placeholder="Type your word">
    <button type="submit" class="btn btn-primary">Play</button>
  </div>
</form>
